new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
djikstra_tracking[reachable_nodes$TO_UFI[update_mask], distance := new_distances[update_mask]]
# Update the unvisited distances and re-sort
updated_nodes <- reachable_nodes$TO_UFI[update_mask]
for(node in updated_nodes) {
unvisited_distances[UFI == node, distance := djikstra_tracking[node]$distance]
}
setorder(unvisited_distances, distance)
}
}
}
djikstra_tracking[, walking_time := distance %/% 84]
djikstra_tracking = djikstra_tracking[visited == T]
})
djikstra <- function(starting_node, max_distance = 46 * 84) {
profvis({
# Set up tracking df
djikstra_tracking <- vertices %>%
st_drop_geometry() %>%
mutate(distance = Inf, visited = F) %>%
as.data.table()
setkey(djikstra_tracking, UFI)
djikstra_tracking[starting_node]$distance <- 0
# Use a simple vector to track unvisited nodes
unvisited_nodes <- djikstra_tracking$UFI
while (length(unvisited_nodes) > 0) {
# Find closest unvisited node
distances <- djikstra_tracking[unvisited_nodes]$distance
min_idx <- which.min(distances)
current_node <- unvisited_nodes[min_idx]
current_tracked_distance <- distances[min_idx]
# Early termination if we reach max distance
if(current_tracked_distance > max_distance) {
break
}
# Remove from unvisited
unvisited_nodes <- unvisited_nodes[-min_idx]
#update master tracking branch
djikstra_tracking[current_node, visited := TRUE]
#find what we can get to
reachable_nodes <- edges_dt[current_node]
#vectorised for speed
if(nrow(reachable_nodes) > 0) {
neighbor_distances <- djikstra_tracking[reachable_nodes$TO_UFI]$distance
new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
djikstra_tracking[reachable_nodes$TO_UFI[update_mask], distance := new_distances[update_mask]]
}
}
}
djikstra_tracking[, walking_time := distance %/% 84]
djikstra_tracking = djikstra_tracking[visited == T]
})
return(djikstra_tracking)
}
profvis({
# Set up tracking df
djikstra_tracking <- vertices %>%
st_drop_geometry() %>%
mutate(distance = Inf, visited = F) %>%
as.data.table()
setkey(djikstra_tracking, UFI)
djikstra_tracking[starting_node]$distance <- 0
# Use a simple vector to track unvisited nodes
unvisited_nodes <- djikstra_tracking$UFI
while (length(unvisited_nodes) > 0) {
# Find closest unvisited node
distances <- djikstra_tracking[unvisited_nodes]$distance
min_idx <- which.min(distances)
current_node <- unvisited_nodes[min_idx]
current_tracked_distance <- distances[min_idx]
# Early termination if we reach max distance
if(current_tracked_distance > max_distance) {
break
}
# Remove from unvisited
unvisited_nodes <- unvisited_nodes[-min_idx]
#update master tracking branch
djikstra_tracking[current_node, visited := TRUE]
#find what we can get to
reachable_nodes <- edges_dt[current_node]
#vectorised for speed
if(nrow(reachable_nodes) > 0) {
neighbor_distances <- djikstra_tracking[reachable_nodes$TO_UFI]$distance
new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
djikstra_tracking[reachable_nodes$TO_UFI[update_mask], distance := new_distances[update_mask]]
}
}
}
djikstra_tracking[, walking_time := distance %/% 84]
djikstra_tracking = djikstra_tracking[visited == T]
})
profvis({
# Set up tracking df
djikstra_tracking <- vertices %>%
st_drop_geometry() %>%
mutate(distance = Inf, visited = F) %>%
as.data.table()
setkey(djikstra_tracking, UFI)
djikstra_tracking[starting_node]$distance <- 0
# Use a simple vector to track unvisited nodes
unvisited_nodes <- djikstra_tracking$UFI
while (length(unvisited_nodes) > 0) {
# Find closest unvisited node
distances <- djikstra_tracking[unvisited_nodes]$distance
min_idx <- which.min(distances)
current_node <- unvisited_nodes[min_idx]
current_tracked_distance <- distances[min_idx]
# Early termination if we reach max distance
if(current_tracked_distance > max_distance) {
break
}
# Remove from unvisited
unvisited_nodes <- unvisited_nodes[-min_idx]
#update master tracking branch
djikstra_tracking[current_node, visited := TRUE]
#find what we can get to
reachable_nodes <- edges_dt[current_node]
#vectorised for speed
if(nrow(reachable_nodes) > 0) {
neighbor_distances <- djikstra_tracking[reachable_nodes$TO_UFI]$distance
new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
djikstra_tracking[reachable_nodes$TO_UFI[update_mask], distance := new_distances[update_mask]]
}
}
}
djikstra_tracking[, walking_time := distance %/% 84]
djikstra_tracking = djikstra_tracking[visited == T]
})
profvis({
# Set up tracking with named vectors for O(1) lookups
distances <- setNames(rep(Inf, nrow(vertices)), vertices$UFI)
distances[starting_node] <- 0
visited <- setNames(rep(FALSE, nrow(vertices)), vertices$UFI)
# Use a simple vector to track unvisited nodes
unvisited_nodes <- vertices$UFI
while (length(unvisited_nodes) > 0) {
# Find closest unvisited node
unvisited_distances <- distances[unvisited_nodes]
min_idx <- which.min(unvisited_distances)
current_node <- unvisited_nodes[min_idx]
current_tracked_distance <- unvisited_distances[min_idx]
# Early termination if we reach max distance
if(current_tracked_distance > max_distance) {
break
}
# Remove from unvisited
unvisited_nodes <- unvisited_nodes[-min_idx]
#update master tracking branch
visited[current_node] <- TRUE
#find what we can get to
reachable_nodes <- edges_dt[current_node]
#vectorised for speed
if(nrow(reachable_nodes) > 0) {
neighbor_distances <- distances[reachable_nodes$TO_UFI]
new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
distances[reachable_nodes$TO_UFI[update_mask]] <- new_distances[update_mask]
}
}
}
# Create result data.table
result <- data.table(
UFI = names(distances)[visited],
distance = distances[visited],
walking_time = distances[visited] %/% 84,
visited = TRUE
)
})
# Set up tracking with named vectors for O(1) lookups
distances <- setNames(rep(Inf, nrow(vertices)), vertices$UFI)
distances[starting_node] <- 0
visited <- setNames(rep(FALSE, nrow(vertices)), vertices$UFI)
# Use a simple vector to track unvisited nodes
unvisited_nodes <- vertices$UFI
while (length(unvisited_nodes) > 0) {
# Find closest unvisited node
unvisited_distances <- distances[unvisited_nodes]
min_idx <- which.min(unvisited_distances)
current_node <- unvisited_nodes[min_idx]
current_tracked_distance <- unvisited_distances[min_idx]
# Early termination if we reach max distance
if(current_tracked_distance > max_distance) {
break
}
# Remove from unvisited
unvisited_nodes <- unvisited_nodes[-min_idx]
#update master tracking branch
visited[current_node] <- TRUE
#find what we can get to
reachable_nodes <- edges_dt[current_node]
#vectorised for speed
if(nrow(reachable_nodes) > 0) {
neighbor_distances <- distances[reachable_nodes$TO_UFI]
new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
distances[reachable_nodes$TO_UFI[update_mask]] <- new_distances[update_mask]
}
}
}
# Create result data.table
result <- data.table(
UFI = names(distances)[visited],
distance = distances[visited],
walking_time = distances[visited] %/% 84,
visited = TRUE
)
View(result)
profvis({
# Set up tracking with named vectors for O(1) lookups
distances <- setNames(rep(Inf, nrow(vertices)), vertices$UFI)
distances[starting_node] <- 0
visited <- setNames(rep(FALSE, nrow(vertices)), vertices$UFI)
# Use a simple vector to track unvisited nodes
unvisited_nodes <- vertices$UFI
while (length(unvisited_nodes) > 0) {
# Find closest unvisited node
unvisited_distances <- distances[unvisited_nodes]
min_idx <- which.min(unvisited_distances)
current_node <- unvisited_nodes[min_idx]
current_tracked_distance <- unvisited_distances[min_idx]
# Early termination if we reach max distance
if(current_tracked_distance > max_distance) {
break
}
# Remove from unvisited
unvisited_nodes <- unvisited_nodes[-min_idx]
#update master tracking branch
visited[current_node] <- TRUE
#find what we can get to
reachable_nodes <- edges_dt[current_node]
#vectorised for speed
if(nrow(reachable_nodes) > 0) {
neighbor_distances <- distances[reachable_nodes$TO_UFI]
new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
distances[reachable_nodes$TO_UFI[update_mask]] <- new_distances[update_mask]
}
}
}
# Create result data.table
result <- data.table(
UFI = names(distances)[visited],
distance = distances[visited],
walking_time = distances[visited] %/% 84,
visited = TRUE
)
})
edges_dt <- bind_rows(edges, edges_reversed) %>%
mutate(FROM_UFI = as.character(FROM_UFI),
TO_UFI = as.character(TO_UFI)) %>%
group_by(FROM_UFI, TO_UFI) %>%
summarise(distance = min(distance), .groups = 'drop') %>%
as.data.table()
edges_list = split(edges_dt, edges_dt$FROM_UFI)
profvis({
# Set up tracking with named vectors for O(1) lookups
distances <- setNames(rep(Inf, nrow(vertices)), vertices$UFI)
distances[starting_node] <- 0
visited <- setNames(rep(FALSE, nrow(vertices)), vertices$UFI)
# Use a simple vector to track unvisited nodes
unvisited_nodes <- vertices$UFI
while (length(unvisited_nodes) > 0) {
# Find closest unvisited node
unvisited_distances <- distances[unvisited_nodes]
min_idx <- which.min(unvisited_distances)
current_node <- unvisited_nodes[min_idx]
current_tracked_distance <- unvisited_distances[min_idx]
# Early termination if we reach max distance
if(current_tracked_distance > max_distance) {
break
}
# Remove from unvisited
unvisited_nodes <- unvisited_nodes[-min_idx]
#update master tracking branch
visited[current_node] <- TRUE
#find what we can get to
reachable_nodes <- edges_list[[current_node]]
#reachable_nodes <- edges_dt[current_node]
#vectorised for speed
if(nrow(reachable_nodes) > 0) {
neighbor_distances <- distances[reachable_nodes$TO_UFI]
new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
distances[reachable_nodes$TO_UFI[update_mask]] <- new_distances[update_mask]
}
}
}
# Create result data.table
result <- data.table(
UFI = names(distances)[visited],
distance = distances[visited],
walking_time = distances[visited] %/% 84,
visited = TRUE
)
})
djikstra("2293277") -> d
dijkstra <- function(starting_node, max_distance = 46 * 84) {
#profvis({
# Set up tracking with named vectors for O(1) lookups
distances <- setNames(rep(Inf, nrow(vertices)), vertices$UFI)
distances[starting_node] <- 0
visited <- setNames(rep(FALSE, nrow(vertices)), vertices$UFI)
# Use a simple vector to track unvisited nodes
unvisited_nodes <- vertices$UFI
while (length(unvisited_nodes) > 0) {
# Find closest unvisited node
unvisited_distances <- distances[unvisited_nodes]
min_idx <- which.min(unvisited_distances)
current_node <- unvisited_nodes[min_idx]
current_tracked_distance <- unvisited_distances[min_idx]
# Early termination if we reach max distance
if(current_tracked_distance > max_distance) {
break
}
# Remove from unvisited
unvisited_nodes <- unvisited_nodes[-min_idx]
#update master tracking branch
visited[current_node] <- TRUE
#find what we can get to
reachable_nodes <- edges_list[[current_node]]
#reachable_nodes <- edges_dt[current_node]
#vectorised for speed
if(nrow(reachable_nodes) > 0) {
neighbor_distances <- distances[reachable_nodes$TO_UFI]
new_distances <- reachable_nodes$distance + current_tracked_distance
update_mask <- new_distances < neighbor_distances & !is.na(neighbor_distances)
if(any(update_mask)) {
distances[reachable_nodes$TO_UFI[update_mask]] <- new_distances[update_mask]
}
}
}
# Create result data.table
result <- data.table(
UFI = names(distances)[visited],
distance = distances[visited],
walking_time = distances[visited] %/% 84,
visited = TRUE
)
#})
return(result)
}
dijkstra("2293277") -> d
dijkstra("2290274") -> d
d %>% filter(visited) %>% left_join(tr_infra_brunswick %>% mutate(UFI = as.character(UFI)), by = "UFI") -> djik_test
write_sf(djik_test, 'sf_output/djikstra_test.gpkg')
#source files
source('calculate_mesh_block_employment.R')
source('calculate_isochrone_employment.R')
source('gtfs_files/initialise_gtfs.R')
source('reset_storage.R')
source('gtfs_files/calculate_walking_distances.R')
source('gtfs_files/generate_place_registry.R')
source('gtfs_files/parallel_fork_processing.R')
#settings - core count and whether to enable parallel processing
#enable parallel processing (required for sub ~4 hour processing time)
doParallel = T
#set the number of cores to work on in parallel
#core count < RAM/5
#e.g 32gb RAM, 6 cores would do, but you cant alloc
num_cores <- min(as.numeric(get_ram()/(10^9)) / 6, parallel::detectCores() - 1)
#num_cores = 12
#files required:
# - mesh blocks
# - destination zones
# - dwelling_data
# - employment csv
#set file paths
mb_path <- '~/Documents/r_projects/shapefiles/MB_2021_AUST_SHP_GDA2020/MB_2021_AUST_GDA2020.shp'
dz_path <- '~/Documents/r_projects/shapefiles/DZN_2021_AUST_GDA2020_SHP/DZN_2021_AUST_GDA2020.shp'
dd_path <- '~/Documents/r_projects/shapefiles/melbourne_dwelling_data.gpkg'
employment_csv_path <- 'sf_input/employment_dzn.csv'
#this script stores isochrone outputs in stop_isochrones, so we need to reset the files as to not mix two gtfs schedules together
reset_storage()
#set gtfs parameters
gtfs_parameters =  list(
mode_numbers = 2:4,
day = 'monday',
city = 'melbourne'
)
isochrone_params = list(
start_time_ = "8:59:00",
time_limit_  = hms("9:45:00"),
xfer_penalty_ = hms("00:05:00")
)
#preload hash tables and gtfs tables for use later
initialise_gtfs(gtfs_parameters, isochrone_params)
#get stops list
gtfs_pre_stops <- unique_stops
#get stops sf
stops_sf <- stops %>%
mutate(stop_id = as.character(stop_id)) %>%
filter(stop_id %in% gtfs_pre_stops)
stops
st_nearest_feature(stops, vertices)
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) )
View(stops_with_ufi)
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
left_join(vertices, by = 'UFI')
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry()
View(stops_with_ufi)
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
left_join(vertices, by = c("nearest_ufi" = 'UFI'))
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
left_join(vertices, by = c('UFI' = 'nearest_UFI'))
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
left_join(vertices, by = c("nearest_UFI" = 'UFI'))
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
mutate(nearest_UFI = as.character(nearest_UFI)) %>%
left_join(vertices, by = c("nearest_UFI" = 'UFI'))
write_sf(stops_with_ufi, 'sf_output/stops_to_ufi.gpkg')
View(stops_with_ufi)
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
mutate(nearest_UFI = as.character(nearest_UFI)) %>%
left_join(vertices, by = c(nearest_UFI = UFI))
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
mutate(nearest_UFI = as.character(nearest_UFI)) %>%
left_join(vertices, by = c(nearest_UFI = 'UFI'))
View(stops_with_ufi)
vertex_lookup <- setNames(1:nrow(vertices), vertices$UFI)
vertex_lookup[1]
vertex_lookup <- setNames( vertices$UFI, 1:nrow(vertices))
vertex_lookup[1]
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
mutate(nearest_UFI = vertex_lookup[nearest_UFI]) %>%
left_join(vertices, by = c(nearest_UFI = 'UFI'))
View(stops_with_ufi)
write_sf(stops_with_ufi, 'sf_output/stops_to_ufi.gpkg')
View(stops_with_ufi)
connect_stops_with_nodes <- function(vertices, stops) {
vertex_lookup <- setNames( vertices$UFI, 1:nrow(vertices))
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
mutate(nearest_UFI = vertex_lookup[nearest_UFI]) %>%
left_join(vertices, by = c(nearest_UFI = 'UFI'))
return(stops_with_ufi)
}
dijkstra("2290274") -> d
d_walk = d
View(stops_with_ufi)
connect_stops_with_nodes <- function(vertices, stops) {
vertex_lookup <- setNames( vertices$UFI, 1:nrow(vertices))
stops_with_ufi <- stops %>% mutate( nearest_UFI =st_nearest_feature(stops, vertices) ) %>%
st_drop_geometry() %>%
mutate(nearest_UFI = vertex_lookup[nearest_UFI]) %>%
left_join(vertices, by = c(nearest_UFI = 'UFI'))
return(stops_with_ufi %>% select(stop_id, stop_name, nearest_UFI) %>% as.data.table())
}
stop_ufi_dict <- connect_stops_with_nodes(vertices, stops)
setkey(stop_ufi_dict, nearest_UFI)
d_transit = d[stop_ufi_dict, on = .(nearest_UFI = TO_UFI)]
View(d)
d_transit = d[stop_ufi_dict, on = .(nearest_UFI = UFI)]
d_transit = d[stop_ufi_dict, on = (nearest_UFI = UFI)]
d_transit = d[stop_ufi_dict, on = ('nearest_UFI' = 'UFI')]
d_transit = d[stop_ufi_dict, on = ('UFI' = 'nearest_UFI' )]
d_transit = d[stop_ufi_dict, on = c('UFI' = 'nearest_UFI' )]
d_transit = stop_ufi_dict[d, on = c('UFI' = 'nearest_UFI' )]
d_transit = stop_ufi_dict[d, on = c('nearest_UFI' = 'UFI' )]
d_transit = d[stop_ufi_dict, on = c('UFI' = 'nearest_UFI' ), nomatch = NULL]
View(d_transit)
d_transit = stop_ufi_dict[d, on = c('nearest_UFI' = 'UFI' ), nomatch = NULL]
dijkstra("2290274") -> d
stop_ufi_dict <- connect_stops_with_nodes(vertices, stops)
setkey(stop_ufi_dict, nearest_UFI)
d_transit = stop_ufi_dict[d, on = c('nearest_UFI' = 'UFI' ), nomatch = NULL]
dijkstra("2290274") -> d
stop_ufi_dict <- connect_stops_with_nodes(vertices, stops)
setkey(stop_ufi_dict, nearest_UFI)
d_walk = d
d_transit = d[, on = c('UFI' = 'nearest_UFI' ), nomatch = NULL]
dijkstra("2290274") -> d
stop_ufi_dict <- connect_stops_with_nodes(vertices, stops)
setkey(stop_ufi_dict, nearest_UFI)
d_walk = d
d_transit = d[stop_ufi_dict, on = c('UFI' = 'nearest_UFI' ), nomatch = NULL]
d_transit = d[UFI %in% stop_ufi_dict$nearest_UFI]
d %>% filter(visited) %>% left_join(tr_infra_brunswick %>% mutate(UFI = as.character(UFI)), by = "UFI") -> djik_test
write_sf(djik_test, 'sf_output/djikstra_test.gpkg')
d_transit %>% filter(visited) %>% left_join(tr_infra_brunswick %>% mutate(UFI = as.character(UFI)), by = "UFI") -> djik_test
write_sf(djik_test, 'sf_output/djikstra_transit_test.gpkg')
res <- d[stop_ufi_dict, on = c("UFI" = "nearest_UFI")]
res <- d[stop_ufi_dict, on = c("UFI" = "nearest_UFI"), nomatch = NULL]
dijkstra("2290274") -> d
stop_ufi_dict <- connect_stops_with_nodes(vertices, stops)
setkey(stop_ufi_dict, nearest_UFI)
res <- d[stop_ufi_dict, on = c("UFI" = "nearest_UFI"), nomatch = NULL]
View(res)
View(d)
View(stop_ufi_dict)
res <- d[stop_ufi_dict, on = c("UFI" = "nearest_UFI"), nomatch = NULL]
length(unique(res$UFI))
